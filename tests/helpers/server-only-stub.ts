/**
 * Stub for the `server-only` guard.
 *
 * That package deliberately throws when imported outside a React Server Component
 * bundler — which is the protection we want in the app and an obstacle in tests,
 * where the server modules are the thing under test. Aliased in vitest.config.mts.
 */
export {};
